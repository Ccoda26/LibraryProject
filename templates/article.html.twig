<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>

    {% for article in articles %}
    <h1>
        <a href="{{ path('articleShow', {'id' : article.id}) }}">{{ article.title }}</a>
    </h1>
    {% endfor %}

</body>
</html>