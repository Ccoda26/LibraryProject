<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<header>
    {# recupere la variable message de la notif flash en gardant que le messsage #}
    {% for message in app.flashes('success') %}
        <p>
            {{ message }}
        </p>
    {% endfor %}
</header>
<p><a href="{{ path('admin_page_insert') }}">Creation divine</a></p>
    {% for article in articles %}
    <h1>
        {{ article.title }}
    </h1>
        <p><a href="{{ path('admin_update_article', {'id' : article.id}) }}">Update ça</a></p>
        <p><a href="{{ path('admin_delete_article', {'id' : article.id}) }}">Supprime ça</a></p>
    {% endfor %}

</body>
</html>