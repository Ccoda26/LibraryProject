<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
   <style>
       a{
       color:black;
       text-decoration: none;
       }
   </style>

</head>
<body>

    {% for category in categories %}
        <h1 >
            <a href="{{ path('categoryShow', {'id' : category.id}) }}" >
                {{ category.title }}
            </a>
        </h1>
        <p><a href="{{ path('update_category', {'id' : category.id}) }}">Update ça</a></p>
    {% endfor %}

</body>
</html>